apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jenkins

resources:
  - deployment.yaml
  - service-account.yaml
  - service.yaml
  - pvc-volume.yaml
  - plugin.yaml
  - jenkins-init-groovy.yaml

  - casc-config
  - jobdsl-config